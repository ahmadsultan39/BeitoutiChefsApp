enum Gender {
  male,
  female,
}

enum AccessibilityStaysType {
  active,
  notApproved,
  notRegistered,
  notVerified,
  blocked,
  isRejected,
}

enum StudentLocation {
  mazzeh,
  hamak,
  barzeh,
}
